{
  "devDependencies": {
    "grunt": "^1.6.1",
    "gulp": "^4.0.2",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "admin-lte": "3.2.0",
    "bootstrap": "4.6.1"
  },
  "scripts": {
    "publish": "npm install && gulp publish"
  }
}
